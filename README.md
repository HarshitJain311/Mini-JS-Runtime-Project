# Mini-JS-Runtime-Project
Mini JavaScript Runtime is a lightweight web app that lets you upload a .js file, preview its contents, and run it from the browser.Dependencies: C++17 toolchain + CMake, and Node.js must be installed (the backend invokes node to execute the uploaded JS).
